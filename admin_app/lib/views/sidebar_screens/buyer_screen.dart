import 'package:flutter/material.dart';

class buyer_screen extends StatelessWidget {
  const buyer_screen({super.key});
  static const String id = '\buyer_screen';

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
